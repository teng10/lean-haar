\section{Solvability of the Weingarten System}

We study the moment computations of operators under the Haar measure, which reduces to solving a system of linear equations. We first analyze this system in an abstract Hilbert-space setting.

\begin{theorem}[Abstract Gram-System Solvability]\label{thm:gram_system_solvable}
  \lean{SchurWeyl.gram_system_solvable}\leanok
  Let $H$ be a finite-dimensional complex Hilbert space, $v_1, \dots, v_n \in H$, and $w \in H$. The linear system:
  \begin{equation}
    \langle v_j, w \rangle = \sum_{i=1}^n c_i \langle v_j, v_i \rangle \quad \text{for all } j
  \end{equation}
  always has at least one solution $c \in \mathbb{C}^n$, which is given by the coefficients of the orthogonal projection of $w$ onto $\text{Span}(v_1, \dots, v_n)$.
\end{theorem}

To apply this to operators on $V^{\otimes k}$, we define permutation operators and their adjoints.

\begin{definition}[Permutation Operators]\label{def:permOp}
  \lean{SchurWeyl.permOp}\uses{def:permAction}\leanok
  The permutation operator $V_d(\sigma) \in \text{End}(V^{\otimes k})$ is the linear map corresponding to $\sigma \in S_k$.
\end{definition}

\begin{definition}[Adjoint Permutation Operators]\label{def:permDual}
  \lean{SchurWeyl.permDual}\uses{def:permAction}\leanok
  The dual permutation operator $V_d^\dagger(\sigma)$ is defined as the permutation operator of $\sigma^{-1}$.
\end{definition}

\begin{theorem}[Hermitian Adjoint Agreement]\label{thm:permDual_eq_conjTranspose}
  \lean{SchurWeyl.permDual_eq_conjTranspose}\uses{def:toEndMatrix, def:permOp, def:permDual, lem:toEndMatrix_permAction}\leanok
  In the computational basis of $V^{\otimes k}$, the matrix representation of $V_d^\dagger(\sigma)$ is the conjugate transpose of the matrix representation of $V_d(\sigma)$.
\end{theorem}

\begin{definition}[Matrix Vectorization]\label{def:endVec}
  \lean{SchurWeyl.endVec}\uses{def:toEndMatrix}\leanok
  The vectorization $\text{endVec}(O)$ of an operator $O \in \text{End}(V^{\otimes k})$ is the vector of its matrix entries in the computational basis, viewed as an element of the Euclidean space.
\end{definition}

\begin{theorem}[The Trace Bridge]\label{thm:inner_endVec_perm_eq_trace}
  \lean{SchurWeyl.inner_endVec_perm_eq_trace}\uses{def:permOp, def:permDual, def:tensorBasis, def:endVec, lem:toEndMatrix_permAction}\leanok
  The Euclidean inner product of the vectorizations of $V_d(\sigma)$ and $O$ equals the trace of their product:
  \begin{equation}
    \langle \text{endVec}(V_d(\sigma)), \text{endVec}(O) \rangle = \text{Tr}(V_d^\dagger(\sigma) O)
  \end{equation}
\end{theorem}

\begin{theorem}[Solvability of Weingarten Linear System]\label{thm:weingarten_linear_system_solvable}
  \lean{SchurWeyl.weingarten_linear_system_solvable}\uses{thm:gram_system_solvable, thm:inner_endVec_perm_eq_trace, def:permOp, def:endVec}\leanok
  For any operator $O \in \text{End}(V^{\otimes k})$, the Weingarten system of $k!$ equations:
  \begin{equation}
    \text{Tr}(V_d^\dagger(\sigma) O) = \sum_{\pi \in S_k} c_\pi \text{Tr}(V_d^\dagger(\sigma) V_d(\pi)) \quad \text{for all } \sigma \in S_k
  \end{equation}
  always has at least one solution $c: S_k \to \mathbb{C}$.
\end{theorem}

\begin{lemma}[Adjoint Trace Linearity]\label{lem:trace_permDual_comp_sum}
  \lean{SchurWeyl.trace_permDual_comp_sum}\uses{def:permDual, def:permOp}\leanok
  The trace against $V_d^\dagger(\sigma)$ is linear in combinations of permutation operators.
\end{lemma}

\begin{lemma}[Operator Span Inclusion]\label{lem:sum_smul_permOp_mem_span}
  \lean{SchurWeyl.sum_smul_permOp_mem_span}\uses{def:permOp, def:permImage}\leanok
  A linear combination of permutation operators lies in their span.
\end{lemma}

\begin{theorem}[Existence of Moment Representation Operator]\label{thm:weingarten_moment_operator_spec}
  \lean{SchurWeyl.weingarten_moment_operator_spec}\uses{thm:weingarten_linear_system_solvable, lem:sum_smul_permOp_mem_span, lem:trace_permDual_comp_sum, def:permOp, def:permImage, def:permDual}\leanok
  There exists a linear combination $M = \sum_{\pi} c_\pi V_d(\pi)$ lying in $\text{Span}(\mathcal{P}_{d,k})$ whose traces match $O$:
  \begin{equation}
    \text{Tr}(V_d^\dagger(\sigma) M) = \text{Tr}(V_d^\dagger(\sigma) O) \quad \text{for all } \sigma \in S_k
  \end{equation}
\end{theorem}

\begin{theorem}[Moment Operator Decomposition from Trace and Span Properties]\label{thm:weingarten_moment_decomposition_of_props}
  \lean{SchurWeyl.weingarten_moment_decomposition_of_props}\uses{lem:trace_permDual_comp_sum, def:permImage, def:permOp, def:permDual}\leanok
  If any operator $M$ lies in $\text{Span}(\mathcal{P}_{d,k})$ and has $\text{Tr}(V_d^\dagger(\sigma) M) = \text{Tr}(V_d^\dagger(\sigma) O)$ for all $\sigma$, then its coefficients solve the Weingarten system.
\end{theorem}


\section{Uniqueness and the Gram Matrix for $k \le d$}

When the number of tensor factors $k$ is at most the dimension $d$, the permutation operators are linearly independent, which implies that the solution to the Weingarten system is unique.

\begin{definition}[Abstract Gram Matrix]\label{def:gramMatrix}
  \lean{SchurWeyl.gramMatrix}\leanok
  The Gram matrix of a family $v$ is $G_{j, i} = \langle v_j, v_i \rangle$.
\end{definition}

\begin{definition}[Abstract Gram Target Vector]\label{def:gramVec}
  \lean{SchurWeyl.gramVec}\leanok
  The Gram target vector is $u_j = \langle v_j, w \rangle$.
\end{definition}

\begin{theorem}[Abstract Gram Matrix Application]\label{thm:gramMatrix_apply}
  \lean{SchurWeyl.gramMatrix_apply}\uses{def:gramMatrix}\leanok
  The entries of the Gram matrix satisfy $\text{gramMatrix}(v)_{j, i} = \langle v_j, v_i \rangle$.
\end{theorem}

\begin{theorem}[Abstract Gram Target Application]\label{thm:gramVec_apply}
  \lean{SchurWeyl.gramVec_apply}\uses{def:gramVec}\leanok
  The entries of the target vector satisfy $\text{gramVec}(v, w)_j = \langle v_j, w \rangle$.
\end{theorem}

\begin{theorem}[Matrix Formulation of Gram System]\label{thm:isGramSolution_iff_mulVec}
  \lean{SchurWeyl.isGramSolution_iff_mulVec}\uses{def:gramMatrix, def:gramVec, thm:gramMatrix_apply, thm:gramVec_apply}\leanok
  A vector $c$ solves the Gram system if and only if $G c = u$.
\end{theorem}

\begin{theorem}[Invertibility of Independent Gram Matrix]\label{thm:gramMatrix_det_ne_zero}
  \lean{SchurWeyl.gramMatrix_det_ne_zero}\uses{def:gramMatrix}\leanok
  If the vectors $v_1, \dots, v_n$ are linearly independent, then their Gram matrix is invertible.
\end{theorem}

\begin{theorem}[Gram Matrix Determinant is a Unit]\label{thm:gramMatrix_isUnit_det}
  \lean{SchurWeyl.gramMatrix_isUnit_det}\uses{def:gramMatrix, thm:gramMatrix_det_ne_zero}\leanok
  If the family of vectors is linearly independent, the determinant of its Gram matrix is a unit in $\mathbb{C}$.
\end{theorem}

\begin{theorem}[Unique Gram Solution]\label{thm:gram_solution_eq_inv}
  \lean{SchurWeyl.gram_solution_eq_inv}\uses{def:gramMatrix, def:gramVec, thm:isGramSolution_iff_mulVec, thm:gramMatrix_isUnit_det}\leanok
  If the vectors are linearly independent, the Gram system has a unique solution given by $c = G^{-1} u$.
\end{theorem}

\begin{definition}[Abstract Gram Solution Set]\label{def:gramSolutionSet}
  \lean{SchurWeyl.gramSolutionSet}\leanok
  The set of all solution vectors $c$ to the Gram system.
\end{definition}

\begin{theorem}[Gram Solution Set is Singleton]\label{thm:gram_solutionSet_eq_singleton}
  \lean{SchurWeyl.gram_solutionSet_eq_singleton}\uses{def:gramSolutionSet, def:gramMatrix, def:gramVec, thm:gramMatrix_isUnit_det, thm:gram_solution_eq_inv, thm:isGramSolution_iff_mulVec}\leanok
  For a linearly independent family of vectors, the solution set of the Gram system is the singleton containing $G^{-1} u$.
\end{theorem}

\begin{theorem}[Uniqueness of Gram System Solution]\label{thm:gram_system_unique}
  \lean{SchurWeyl.gram_system_unique}\uses{thm:gram_system_solvable, thm:gram_solution_eq_inv}\leanok
  For a linearly independent family of vectors, there exists a unique solution to the Gram system.
\end{theorem}

We apply this to permutation operators.

\begin{definition}[Matrix Entries Equivalence]\label{def:matrixEntriesEquiv}
  \lean{SchurWeyl.matrixEntriesEquiv}\leanok
  The linear equivalence between matrices and the function space of their entries.
\end{definition}

\begin{definition}[Endomorphism Vectorization Equivalence]\label{def:endVecEquiv}
  \lean{SchurWeyl.endVecEquiv}\uses{def:toEndMatrix, def:matrixEntriesEquiv}\leanok
  The linear equivalence between endomorphisms and the Euclidean space of their matrix entries.
\end{definition}

\begin{definition}[Linear Endomorphism Vectorization]\label{def:endVecₗ}
  \lean{SchurWeyl.endVecₗ}\uses{def:endVecEquiv}\leanok
  The linear map representing the vectorization of endomorphisms.
\end{definition}

\begin{theorem}[Linear Vectorization Application]\label{thm:endVecₗ_apply}
  \lean{SchurWeyl.endVecₗ_apply}\uses{def:endVecₗ, def:endVec}\leanok
  The linear endomorphism vectorization matches the standard vectorization: $\text{endVec}_\ell(O) = \text{endVec}(O)$.
\end{theorem}

\begin{theorem}[Linear Vectorization is Injective]\label{thm:endVecₗ_injective}
  \lean{SchurWeyl.endVecₗ_injective}\uses{def:endVecₗ, def:endVecEquiv}\leanok
  The linear endomorphism vectorization is injective.
\end{theorem}

\begin{theorem}[Linear Vectorization Kernel]\label{thm:endVecₗ_ker}
  \lean{SchurWeyl.endVecₗ_ker}\uses{def:endVecₗ, thm:endVecₗ_injective}\leanok
  The kernel of the linear endomorphism vectorization is the trivial submodule.
\end{theorem}

\begin{theorem}[Linear Independence of Vectorized Permutations]\label{thm:linearIndependent_endVec_permOp}
  \lean{SchurWeyl.linearIndependent_endVec_permOp}\uses{def:endVec, def:permOp, lem:toEndMatrix_permAction}\leanok
  For $k \le d$, the vectorized permutation operators $\text{endVec}(V_d(\sigma))$ are linearly independent.
\end{theorem}

\begin{theorem}[Linear Independence of Permutation Operators]\label{thm:linearIndependent_permOp}
  \lean{SchurWeyl.linearIndependent_permOp}\uses{def:permOp, thm:linearIndependent_endVec_permOp, def:endVecₗ, thm:endVecₗ_apply}\leanok
  For $k \le d$, the permutation operators $V_d(\sigma)$ are linearly independent in $\text{End}(V^{\otimes k})$.
\end{theorem}

\begin{definition}[Weingarten Gram Matrix]\label{def:weingartenGram}
  \lean{SchurWeyl.weingartenGram}\uses{def:permDual, def:permOp}\leanok
  The Weingarten Gram matrix is defined by $G_{\sigma, \pi} = \text{Tr}(V_d^\dagger(\sigma) V_d(\pi))$.
\end{definition}

\begin{definition}[Weingarten Target Vector]\label{def:weingartenVec}
  \lean{SchurWeyl.weingartenVec}\uses{def:permDual}\leanok
  The Weingarten target vector is $u_\sigma = \text{Tr}(V_d^\dagger(\sigma) O)$.
\end{definition}

\begin{theorem}[Weingarten Gram as Vectorized Gram]\label{thm:weingartenGram_eq_gramMatrix}
  \lean{SchurWeyl.weingartenGram_eq_gramMatrix}\uses{def:weingartenGram, def:gramMatrix, thm:inner_endVec_perm_eq_trace, def:endVec, def:permOp, thm:gramMatrix_apply}\leanok
  The Weingarten Gram matrix is the Gram matrix of vectorized permutation operators.
\end{theorem}

\begin{theorem}[Weingarten Target as Vectorized Target]\label{thm:weingartenVec_eq_gramVec}
  \lean{SchurWeyl.weingartenVec_eq_gramVec}\uses{def:weingartenVec, def:gramVec, thm:inner_endVec_perm_eq_trace, def:endVec, def:permOp, thm:gramVec_apply}\leanok
  The Weingarten target vector is the Gram vector of vectorized permutation operators.
\end{theorem}

\begin{theorem}[Cycle Count Formula for Gram Entries]\label{thm:weingartenGram_eq_card}
  \lean{SchurWeyl.weingartenGram_eq_card}\uses{def:weingartenGram, def:endVec, lem:toEndMatrix_permAction, thm:inner_endVec_perm_eq_trace, def:permOp}\leanok
  The entry $\text{Tr}(V_d^\dagger(\sigma) V_d(\pi))$ equals the number of index functions invariant under $\sigma^{-1}\pi$, which evaluates to $d^{\# \text{cycles}(\sigma^{-1}\pi)}$.
\end{theorem}

\begin{definition}[Natural Weingarten Gram Matrix]\label{def:weingartenGramNat}
  \lean{SchurWeyl.weingartenGramNat}\leanok
  The natural-number valued Weingarten Gram matrix whose entries are the coincidence counts.
\end{definition}

\begin{theorem}[Weingarten Gram is NatCast]\label{thm:weingartenGram_eq_natCast}
  \lean{SchurWeyl.weingartenGram_eq_natCast}\uses{def:weingartenGram, def:weingartenGramNat, thm:weingartenGram_eq_card}\leanok
  The complex Weingarten Gram matrix entries are cast from the natural-number valued Weingarten Gram matrix.
\end{theorem}

\begin{theorem}[Natural Weingarten Gram Diagonal]\label{thm:weingartenGramNat_diag}
  \lean{SchurWeyl.weingartenGramNat_diag}\uses{def:weingartenGramNat}\leanok
  The diagonal entries of the natural Weingarten Gram matrix equal $d^k$.
\end{theorem}

\begin{theorem}[Invertibility of Weingarten Gram Matrix]\label{thm:weingartenGram_det_ne_zero}
  \lean{SchurWeyl.weingartenGram_det_ne_zero}\uses{def:weingartenGram, thm:weingartenGram_eq_gramMatrix, thm:linearIndependent_endVec_permOp, thm:gramMatrix_det_ne_zero}\leanok
  For $k \le d$, the Weingarten Gram matrix is invertible.
\end{theorem}

\begin{definition}[Weingarten Solution Set]\label{def:weingartenSolutionSet}
  \lean{SchurWeyl.weingartenSolutionSet}\uses{def:weingartenGram, def:weingartenVec}\leanok
  The set of solution vectors $c$ to the Weingarten system.
\end{definition}

\begin{theorem}[Weingarten Solution Set is Singleton]\label{thm:weingarten_solutionSet_eq_singleton}
  \lean{SchurWeyl.weingarten_solutionSet_eq_singleton}\uses{def:weingartenSolutionSet, def:weingartenGram, def:weingartenVec, thm:gram_solutionSet_eq_singleton, thm:linearIndependent_endVec_permOp, thm:weingartenGram_eq_gramMatrix, thm:weingartenVec_eq_gramVec}\leanok
  For $k \le d$, the solution set of the Weingarten system is a singleton containing $G^{-1} u$.
\end{theorem}

\begin{theorem}[Unique Weingarten Solution]\label{thm:weingarten_solution_unique}
  \lean{SchurWeyl.weingarten_solution_unique}\uses{def:weingartenVec, def:weingartenGram, thm:linearIndependent_endVec_permOp, thm:weingartenGram_eq_gramMatrix, thm:weingartenVec_eq_gramVec, thm:gram_system_unique}\leanok
  For $k \le d$, the Weingarten system has a unique solution.
\end{theorem}

\begin{theorem}[Inverse Solution Form]\label{thm:weingarten_solution_eq_inv}
  \lean{SchurWeyl.weingarten_solution_eq_inv}\uses{def:weingartenVec, def:weingartenGram, thm:weingarten_solutionSet_eq_singleton}\leanok
  For $k \le d$, the unique coefficient vector is given by $c = G^{-1} u$.
\end{theorem}

\section{The Haar Moment Operator}

We define the normalized Haar measure on the unitary group and use it to define the moment operator.

\begin{theorem}[Unitary Entry Norm Bound]\label{thm:unitary_entry_norm_le_one}
  \lean{SchurWeyl.unitary_entry_norm_le_one}\leanok
  Each entry of a unitary matrix $M \in U(d)$ has norm at most $1$.
\end{theorem}

\begin{theorem}[Compactness of the Unitary Group]\label{thm:isCompact_unitaryGroup}
  \lean{SchurWeyl.isCompact_unitaryGroup}\uses{thm:unitary_entry_norm_le_one}\leanok
  The unitary group $U(d)$ is a compact subset of the space of $d \times d$ complex matrices.
\end{theorem}

\begin{theorem}[Compact Space Instance of Unitary Group]\label{thm:instCompactSpaceUnitaryGroup}
  \lean{SchurWeyl.instCompactSpaceUnitaryGroup}\uses{thm:isCompact_unitaryGroup}\leanok
  The unitary group $U(d)$ is a compact topological space.
\end{theorem}

\begin{theorem}[Second Countability of Matrix Space]\label{thm:instSecondCountableMatrix}
  \lean{SchurWeyl.instSecondCountableMatrix}\leanok
  The space of $d \times d$ complex matrices has second countable topology.
\end{theorem}

\begin{theorem}[Measurable Space Instance of Unitary Group]\label{thm:instMeasurableSpaceUnitaryGroup}
  \lean{SchurWeyl.instMeasurableSpaceUnitaryGroup}\leanok
  The unitary group $U(d)$ carries the Borel $\sigma$-algebra.
\end{theorem}

\begin{theorem}[Borel Space Instance of Unitary Group]\label{thm:instBorelSpaceUnitaryGroup}
  \lean{SchurWeyl.instBorelSpaceUnitaryGroup}\leanok
  The unitary group $U(d)$ is a Borel space.
\end{theorem}

\begin{theorem}[Haar Measure of Universe is Positive]\label{thm:haar_univ_ne_zero}
  \lean{SchurWeyl.haar_univ_ne_zero}\leanok
  The left Haar measure of the entire unitary group is positive.
\end{theorem}

\begin{theorem}[Haar Measure of Universe is Finite]\label{thm:haar_univ_ne_top}
  \lean{SchurWeyl.haar_univ_ne_top}\leanok
  The left Haar measure of the compact unitary group is finite.
\end{theorem}

\begin{definition}[Haar Probability Measure]\label{def:haarProb}
  \lean{SchurWeyl.haarProb}\leanok
  The Haar probability measure $\mu_H$ is the left Haar measure on the compact unitary group $U(d)$ normalized to total mass 1.
\end{definition}

\begin{theorem}[Haar Probability is Probability Measure]\label{thm:instIsProbabilityMeasureHaarProb}
  \lean{SchurWeyl.instIsProbabilityMeasureHaarProb}\uses{def:haarProb}\leanok
  The Haar probability measure $\mu_H$ is indeed a probability measure (has total mass 1).
\end{theorem}

\begin{theorem}[Haar Probability is Left Invariant]\label{thm:instIsMulLeftInvariantHaarProb}
  \lean{SchurWeyl.instIsMulLeftInvariantHaarProb}\uses{def:haarProb}\leanok
  The Haar probability measure $\mu_H$ is left-invariant under multiplication.
\end{theorem}

\begin{definition}[Endomorphism of Matrix]\label{def:endOf}
  \lean{SchurWeyl.endOf}\leanok
  The representation of a matrix $U \in \text{Mat}_{d \times d}(\mathbb{C})$ as a linear endomorphism of $\mathbb{C}^d$.
\end{definition}

\begin{theorem}[Multiplicativity of endOf]\label{thm:endOf_mul}
  \lean{SchurWeyl.endOf_mul}\uses{def:endOf}\leanok
  The map `endOf` satisfies $\text{endOf}(A B) = \text{endOf}(A) \circ \text{endOf}(B)$.
\end{theorem}

\begin{theorem}[Identity Matrix endOf]\label{thm:endOf_one}
  \lean{SchurWeyl.endOf_one}\uses{def:endOf}\leanok
  The map `endOf` maps the identity matrix to the identity endomorphism.
\end{theorem}

\begin{theorem}[Diagonal Action Composition]\label{thm:diagAction_comp}
  \lean{SchurWeyl.diagAction_comp}\uses{def:diagAction}\leanok
  The diagonal action is multiplicative: $g^{\otimes k} \circ h^{\otimes k} = (g \circ h)^{\otimes k}$.
\end{theorem}

\begin{theorem}[Diagonal Action Identity]\label{thm:diagAction_id}
  \lean{SchurWeyl.diagAction_id}\uses{def:diagAction}\leanok
  The diagonal action of the identity endomorphism is the identity on the tensor space.
\end{theorem}

\begin{definition}[Conjugation Action]\label{def:actOn}
  \lean{SchurWeyl.actOn}\uses{def:diagAction, def:endOf}\leanok
  For $O \in \text{End}(V^{\otimes k})$ and $U \in U(d)$, the conjugation action is:
  \begin{equation}
    U^{\otimes k} O (U^\dagger)^{\otimes k}
  \end{equation}
\end{definition}

\begin{theorem}[Unitary Diagonal Action Left Inverse]\label{thm:diagAction_endOf_unitary_left}
  \lean{SchurWeyl.diagAction_endOf_unitary_left}\uses{def:endOf, def:diagAction, thm:diagAction_comp, thm:endOf_mul, thm:endOf_one, thm:diagAction_id}\leanok
  For any unitary matrix $U$, $(U^\dagger)^{\otimes k} \circ U^{\otimes k} = \text{id}$.
\end{theorem}

\begin{theorem}[Unitary Diagonal Action Right Inverse]\label{thm:diagAction_endOf_unitary_right}
  \lean{SchurWeyl.diagAction_endOf_unitary_right}\uses{def:endOf, def:diagAction, thm:diagAction_comp, thm:endOf_mul, thm:endOf_one, thm:diagAction_id}\leanok
  For any unitary matrix $U$, $U^{\otimes k} \circ (U^\dagger)^{\otimes k} = \text{id}$.
\end{theorem}

\begin{theorem}[Continuity of Conjugation Action Entries]\label{thm:continuous_actOn_entry}
  \lean{SchurWeyl.continuous_actOn_entry}\uses{def:actOn, def:toEndMatrix, def:diagAction, lem:toEndMatrix_diagAction}\leanok
  For any operator $O \in \text{End}(V^{\otimes k})$, the matrix entries of the conjugation action $\text{actOn}(O, U)$ are continuous functions of $U \in U(d)$.
\end{theorem}

\begin{theorem}[Integrability of Conjugation Action Entries]\label{thm:integrable_actOn_entry}
  \lean{SchurWeyl.integrable_actOn_entry}\uses{thm:continuous_actOn_entry, thm:instCompactSpaceUnitaryGroup, def:haarProb, def:toEndMatrix, def:actOn}\leanok
  Each matrix entry of the conjugation action is integrable with respect to the Haar probability measure.
\end{theorem}

\begin{definition}[Haar Moment Matrix]\label{def:momentMatrix}
  \lean{SchurWeyl.momentMatrix}\uses{def:actOn, def:toEndMatrix, def:haarProb}\leanok
  The matrix formed by integrating the entries of the conjugation action over the unitary group.
\end{definition}

\begin{definition}[Haar Moment Operator]\label{def:momentOp}
  \lean{SchurWeyl.momentOp}\uses{def:momentMatrix, def:toEndMatrix}\leanok
  The Haar moment operator $\mathbb{E}_{U \sim \mu_H}[U^{\otimes k} O (U^\dagger)^{\otimes k}]$, defined as the operator whose matrix in the computational basis is the Haar average of the conjugation action.
\end{definition}

\begin{theorem}[Matrix of Haar Moment Operator]\label{thm:toEndMatrix_momentOp}
  \lean{SchurWeyl.toEndMatrix_momentOp}\uses{def:momentOp, def:momentMatrix, def:toEndMatrix}\leanok
  The matrix representation of the Haar moment operator matches the Haar moment matrix.
\end{theorem}

\begin{theorem}[Pointwise Trace Invariance under Conjugation]\label{thm:trace_permDual_actOn}
  \lean{SchurWeyl.trace_permDual_actOn}\uses{def:permDual, def:actOn, def:diagAction, def:endOf,thm:diagAction_endOf_unitary_left, thm:permAction_diagAction_comm}\leanok
  The trace against $V_d^\dagger(\sigma)$ is invariant under unitary conjugation of the operator.
\end{theorem}

\begin{theorem}[Trace Invariance of Moment Operator]\label{thm:momentOp_trace}
  \lean{SchurWeyl.momentOp_trace}\uses{def:momentOp, def:permDual, def:toEndMatrix, def:actOn, thm:trace_permDual_actOn, thm:toEndMatrix_momentOp, thm:integrable_actOn_entry, def:haarProb}\leanok
  The moment operator preserves the traces:
  \begin{equation}
    \text{Tr}(V_d^\dagger(\sigma) \mathbb{E}[U^{\otimes k} O (U^\dagger)^{\otimes k}]) = \text{Tr}(V_d^\dagger(\sigma) O)
  \end{equation}
\end{theorem}

\begin{theorem}[Translation of Conjugation Action]\label{thm:conj_actOn}
  \lean{SchurWeyl.conj_actOn}\uses{def:diagAction, def:endOf, def:actOn, thm:diagAction_comp}\leanok
  Conjugating the action by a unitary matrix $W$ corresponds to left translating the variable in the conjugation action: $W^{\otimes k} (\text{actOn}(O, U)) (W^\dagger)^{\otimes k} = \text{actOn}(O, W U)$.
\end{theorem}

\begin{theorem}[Unitary Invariance of Haar Moment Operator]\label{thm:momentOp_conj_unitary}
  \lean{SchurWeyl.momentOp_conj_unitary}\uses{def:actOn, def:endOf, thm:diagAction_comp, def:diagAction, thm:endOf_mul, def:momentOp, def:momentMatrix, thm:toEndMatrix_momentOp, def:toEndMatrix, def:tensorBasis, def:haarProb, thm:integrable_actOn_entry, thm:conj_actOn}\leanok
  The Haar moment operator is invariant under conjugation by any unitary matrix.
\end{theorem}

\begin{theorem}[Unitary Commutativity of Moment Operator]\label{thm:momentOp_comm_unitary}
  \lean{SchurWeyl.momentOp_comm_unitary}\uses{def:momentOp, def:diagAction, def:endOf, thm:momentOp_conj_unitary, thm:diagAction_endOf_unitary_left}\leanok
  The moment operator commutes with $W^{\otimes k}$ for any unitary matrix $W \in U(d)$.
\end{theorem}

We extend this to all matrices using density.

\begin{theorem}[Orthonormal Basis from Matrix Columns]\label{thm:exists_orthonormalBasis_cols}
  \lean{SchurWeyl.exists_orthonormalBasis_cols}\leanok
  An orthonormal family can be constructed from the columns of a matrix $N$ when normalized.
\end{theorem}

\begin{theorem}[Spectral Decomposition of Hermetian Matrix]\label{thm:svd_spectral}
  \lean{SchurWeyl.svd_spectral}\leanok
  Any positive semidefinite Hermetian matrix $M^\dagger M$ can be diagonalized by a unitary matrix.
\end{theorem}

\begin{theorem}[Conjugate Transpose Multiplicative Spectral Relation]\label{thm:conjTranspose_mul_self_of_spectral}
  \lean{SchurWeyl.conjTranspose_mul_self_of_spectral}\uses{thm:svd_spectral}\leanok
  If $M^\dagger M = Q D Q^\dagger$, then $(M Q)^\dagger (M Q) = D$.
\end{theorem}

\begin{theorem}[Unitary Matrix from Orthonormal Basis]\label{thm:exists_unitary_of_orthonormalBasis}
  \lean{SchurWeyl.exists_unitary_of_orthonormalBasis}\leanok
  An orthonormal basis can be assembled into a unitary matrix.
\end{theorem}

\begin{theorem}[Column Normalization Relation]\label{thm:cols_mul_diag_sqrt}
  \lean{SchurWeyl.cols_mul_diag_sqrt}\leanok
  The unitary matrix times the diagonal square root matrix recovers the normalized columns.
\end{theorem}

\begin{theorem}[SVD Existence]\label{thm:svd_exists}
  \lean{SchurWeyl.svd_exists}\uses{thm:svd_spectral, thm:conjTranspose_mul_self_of_spectral, thm:exists_orthonormalBasis_cols, thm:exists_unitary_of_orthonormalBasis, thm:cols_mul_diag_sqrt}\leanok
  Any complex matrix can be written as $M = U_1 D U_2$ with $U_1, U_2 \in U(d)$ and diagonal $D$.
\end{theorem}

\begin{theorem}[Matrix of Diagonal Diagonal Action]\label{thm:toEndMatrix_diagAction_diagonal}
  \lean{SchurWeyl.toEndMatrix_diagAction_diagonal}\uses{def:toEndMatrix, def:diagAction, def:endOf, lem:diagAction_diagonal}\leanok
  The matrix representation of the diagonal action of a diagonal matrix is diagonal in the tensor basis.
\end{theorem}

\begin{theorem}[Diagonal Matrix Unitary Criterion]\label{thm:diagonal_mem_unitaryGroup}
  \lean{SchurWeyl.diagonal_mem_unitaryGroup}\leanok
  A diagonal matrix with entries on the unit circle is unitary.
\end{theorem}

\begin{theorem}[Identity extension from Torus]\label{thm:prod_eq_of_torus}
  \lean{SchurWeyl.prod_eq_of_torus}\leanok
  An identity of index-monomials holding on the unit torus holds on all inputs.
\end{theorem}

\begin{theorem}[Matrix of Composition]\label{thm:toEndMatrix_comp}
  \lean{SchurWeyl.toEndMatrix_comp}\uses{def:toEndMatrix, def:tensorBasis}\leanok
  The matrix representation map is multiplicative: $\text{toEndMatrix}(f \circ g) = \text{toEndMatrix}(f) \text{toEndMatrix}(g)$.
\end{theorem}

\begin{theorem}[Entrywise Commutation with Diagonal Action]\label{thm:comm_diagAction_diagonal_entry}
  \lean{SchurWeyl.comm_diagAction_diagonal_entry}\uses{def:diagAction, def:endOf, thm:toEndMatrix_comp, thm:toEndMatrix_diagAction_diagonal, thm:diagonal_mem_unitaryGroup, thm:prod_eq_of_torus}\leanok
  An operator commuting with all unitary diagonal actions commutes entrywise with all diagonal actions.
\end{theorem}

\begin{theorem}[Commutation with all Diagonal Actions]\label{thm:comm_diagAction_diagonal}
  \lean{SchurWeyl.comm_diagAction_diagonal}\uses{def:diagAction, def:endOf, def:toEndMatrix, thm:comm_diagAction_diagonal_entry, thm:toEndMatrix_comp, thm:toEndMatrix_diagAction_diagonal}\leanok
  An operator commuting with all unitary diagonal actions commutes with the diagonal action of any diagonal matrix.
\end{theorem}

\begin{theorem}[Unitary Commutant implies Centralizer]\label{thm:mem_centralizer_of_comm_unitary}
  \lean{SchurWeyl.mem_centralizer_of_comm_unitary}\uses{def:diagAction, thm:svd_exists, thm:comm_diagAction_diagonal, def:endOf, thm:endOf_mul, thm:diagAction_comp}\leanok
  An operator that commutes with all unitary diagonal actions commutes with all diagonal actions.
\end{theorem}

\begin{theorem}[Moment Operator lies in Permutation Span]\label{thm:momentOp_mem_span}
  \lean{SchurWeyl.momentOp_mem_span}\uses{def:momentOp, thm:momentOp_comm_unitary, thm:mem_centralizer_of_comm_unitary, thm:schur_weyl}\leanok
  The Haar moment operator lies in the span of the permutation operators:
  \begin{equation}
    \mathbb{E}_{U}[U^{\otimes k} O (U^\dagger)^{\otimes k}] \in \text{Span}(\mathcal{P}_{d,k})
  \end{equation}
\end{theorem}

\begin{theorem}[Weingarten Moments for Haar Integral]\label{thm:weingarten_moment_haar}
  \lean{SchurWeyl.weingarten_moment_haar}\uses{def:momentOp, def:permOp, def:permDual, thm:momentOp_mem_span, thm:momentOp_trace, thm:weingarten_moment_decomposition_of_props}\leanok
  The Haar moment operator is a linear combination of the permutation operators whose coefficients solve the Weingarten system.
\end{theorem}
